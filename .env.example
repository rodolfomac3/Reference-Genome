# Copy to `.env` and set your email (used by NCBI E-utilities etiquette)
ENTREZ_EMAIL=your.email@domain
# Optional: adjust polite delay between NCBI calls (seconds)
NCBI_DELAY=0.34
# Flask
FLASK_ENV=production
